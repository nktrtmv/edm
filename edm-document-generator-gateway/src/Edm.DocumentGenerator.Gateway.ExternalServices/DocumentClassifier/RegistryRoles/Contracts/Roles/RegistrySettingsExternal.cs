namespace Edm.DocumentGenerator.Gateway.ExternalServices.DocumentClassifier.RegistryRoles.Contracts.Roles;

public sealed record RegistrySettingsExternal(
    bool ShowInRegistry,
    bool ShowByDefault);
